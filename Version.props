<Project>

  <!-- The single place the harness release number is written down. A repository pins a
       release in its frame, so the binary, the Git tag and the published asset all read
       this value rather than restating it. -->
  <PropertyGroup>
    <HarnessVersion>2.4.0</HarnessVersion>
  </PropertyGroup>

</Project>
